#!/bin/bash

if command -v rbenv >/dev/null && rbenv which dotenv >/dev/null 2>&1; then
  exec $(rbenv which dotenv) "$@"
else
  while [[ $# -gt 0 && $1 =~ -.* ]]; do
    if [[ $1 == "-f" ]]; then
      shift
    fi
    shift
  done

  exec "$@"
fi
